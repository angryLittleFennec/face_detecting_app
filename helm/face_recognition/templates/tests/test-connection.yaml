apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "face_recognition.fullname" . }}-test-connection"
  labels:
    {{- include "face_recognition.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "face_recognition.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
